// Tuple in TypeScript
// A tuple in TypeScript is a special type of array where you specify:

// How many elements it should have
// What type each element should be
// The order of those types

const games: [string, string, number] = ["Game 1", "Game 2", 12];

console.log(games);

let person: [string, number];

person = ["John", 25];
